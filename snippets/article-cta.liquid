{% comment %}
  Renders a call-to-action block within blog articles.

  Accepts:
  - cta_title: {String} CTA heading (optional, default: "Ready to Experience Relief?")
  - cta_text: {String} CTA description text (optional)
  - cta_url: {String} CTA button link (optional, default: /collections/all)
  - cta_button_text: {String} Button label (optional, default: "Shop Now")
  - cta_style: {String} Style variant: "default", "highlight", "minimal" (optional, default: "default")

  Usage:
  {% render 'article-cta',
    cta_title: "Find Your Perfect Knee Brace",
    cta_text: "Explore our range of professional-grade knee braces for pain relief and injury recovery.",
    cta_url: "/collections/pain-relief-recovery",
    cta_button_text: "Browse Knee Braces"
  %}
{% endcomment %}

{%- liquid
  assign title = cta_title | default: "Ready to Experience Relief?"
  assign text = cta_text | default: "Explore our range of professional medical support equipment designed to help you feel better faster."
  assign url = cta_url | default: "/collections/all"
  assign button_text = cta_button_text | default: "Shop Now"
  assign style = cta_style | default: "default"
-%}

<div class="article-cta article-cta--{{ style }}" role="region" aria-label="Product recommendation">
  <div class="article-cta__container">
    <div class="article-cta__content">
      <h3 class="article-cta__title">{{ title }}</h3>
      {%- if text != blank -%}
        <p class="article-cta__text">{{ text }}</p>
      {%- endif -%}
      <a href="{{ url }}" class="article-cta__button button button--primary">
        {{ button_text }}
      </a>
    </div>
  </div>
</div>

<style>
.article-cta {
  margin: 2.5rem 0;
  padding: 0;
}

.article-cta__container {
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
  border-left: 4px solid #4770DB;
  border-radius: 8px;
  padding: 2rem;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.article-cta__container:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
}

.article-cta__content {
  text-align: center;
  max-width: 600px;
  margin: 0 auto;
}

.article-cta__title {
  font-size: 1.5rem;
  font-weight: 700;
  color: #212529;
  margin: 0 0 1rem;
  line-height: 1.3;
}

.article-cta__text {
  font-size: 1rem;
  color: #495057;
  margin: 0 0 1.5rem;
  line-height: 1.6;
}

.article-cta__button {
  display: inline-block;
  padding: 14px 32px;
  font-size: 16px;
  font-weight: 600;
  text-decoration: none;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.article-cta__button:hover {
  transform: scale(1.05);
  box-shadow: 0 6px 20px rgba(71, 112, 219, 0.3);
}

/* Highlight variant */
.article-cta--highlight .article-cta__container {
  background: linear-gradient(135deg, #4770DB 0%, #6a8eec 100%);
  border-left-color: #2d4a9f;
}

.article-cta--highlight .article-cta__title {
  color: #ffffff;
}

.article-cta--highlight .article-cta__text {
  color: #f8f9fa;
}

.article-cta--highlight .article-cta__button {
  background: #ffffff;
  color: #4770DB;
}

.article-cta--highlight .article-cta__button:hover {
  background: #f8f9fa;
  color: #2d4a9f;
}

/* Minimal variant */
.article-cta--minimal .article-cta__container {
  background: transparent;
  border: 2px solid #e9ecef;
  border-left-width: 4px;
  box-shadow: none;
}

/* Mobile responsive */
@media screen and (max-width: 749px) {
  .article-cta__container {
    padding: 1.5rem 1rem;
  }

  .article-cta__title {
    font-size: 1.25rem;
  }

  .article-cta__text {
    font-size: 0.9375rem;
  }

  .article-cta__button {
    padding: 12px 24px;
    font-size: 15px;
    width: 100%;
  }
}
</style>
